---
id: TASK-032
title: >-
  logging GUI controls: detailed-logging toggle, open log folder, export logs
  zip
status: Done
assignee:
  - '@claude'
created_date: '2026-06-17 13:47'
updated_date: '2026-06-17 14:18'
labels:
  - feature
  - logging-troubleshooting
  - ready-for-agent
dependencies:
  - TASK-030
priority: high
ordinal: 43000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Part 2/2 of the logging-troubleshooting plan (shared label: logging-troubleshooting). Depends on TASK-030 core logging infra.

GUI controls so a non-technical user can be talked through enabling detailed logs and sending them:
- 'Detailed logging (debug)' checkbox in Settings, bound to the debug_logging config field added in TASK-030 (off by default). Toggling applies live (re-set root level) consistent with existing live-apply settings.
- 'Open log folder' button: opens the OS file manager at user_log_dir('romhop').
- 'Export logs…' button: QFileDialog save -> zip of current + rotated log files to a user-chosen path (default Desktop), filename like romhop-logs.zip.
- Inject backend seams as callables per the GUI DI pattern (app.run wires functions; widgets stay thin, import no backend). e.g. open_log_dir_fn, export_logs_fn.

Decisions from grill-with-docs 2026-06-17 (see TASK-030 for full plan).
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Settings has a 'Detailed logging (debug)' checkbox bound to debug_logging; toggling it applies live and persists
- [x] #2 'Open log folder' opens the OS file manager at the diagnostic log directory
- [x] #3 'Export logs…' writes a zip of current + rotated logs to a user-chosen location
- [x] #4 Backend behaviour injected as callables via gui.run(); widgets import no backend modules (DI boundary preserved)
- [x] #5 pytest-qt tests cover the toggle wiring and that export/open invoke the injected callables
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. logging_setup.py: add get_log_dir(), export_logs(dest_path) (zip current+rotated files)
2. settings_view.py: add open_log_dir_fn/export_logs_fn ctor params; add Open log folder + Export logs buttons; widget opens QFileDialog for export dest then calls fn
3. main_window.py: add open_log_dir_fn/export_logs_fn params, pass to SettingsView
4. app.py: define open_log_dir_fn (QDesktopServices) + export_logs_fn (logging_setup.export_logs) closures; fix apply_settings to reconfigure_logging when debug_logging toggles; pass both to MainWindow
5. tests/test_gui_views.py: test debug_logging toggle persists/live-applies; Open+Export invoke injected callables
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Executor hint (added post-grill): the 'Detailed logging (debug)' checkbox is AUTO-RENDERED by the SCHEMA-driven settings form once TASK-030 adds the debug_logging FieldSpec — do NOT hand-build a separate checkbox. This task's real new UI = the two buttons ('Open log folder', 'Export logs…') plus confirming the auto-checkbox persists + live-applies (re-set root log level on toggle) like other settings. Wire open_log_dir_fn/export_logs_fn as injected callables in gui.run().

Implemented: get_log_dir()+export_logs() in logging_setup.py; Open log folder + Export logs buttons in settings_view.py (injected callables); live debug_logging toggle via apply_settings in app.py; 6 new pytest-qt tests (421 pass). Committed 29d5ade.
<!-- SECTION:NOTES:END -->

## Comments

<!-- COMMENTS:BEGIN -->
author: triage-agent
created: 2026-06-17 13:47
---
> *This was generated by AI during triage.*

## Agent Brief — logging GUI controls (2/2)

Specced via grill-with-docs 2026-06-17. Depends on TASK-030 (core infra: debug_logging field + log dir). Don't start before 030 lands.

**Why:** the core log is useless to a non-technical user who can't find or enable it. These controls let you say 'tick the box, reproduce, click Export, send me the zip'.

**Build (GUI DI pattern):** Settings checkbox 'Detailed logging (debug)' bound to `debug_logging`, live-apply like existing settings. 'Open log folder' -> OS file manager at user_log_dir. 'Export logs…' -> QFileDialog save -> zip of current+rotated logs (default Desktop, romhop-logs.zip). Wire backend as callables in `gui.run()` (e.g. open_log_dir_fn, export_logs_fn); widgets import no backend.

**Test (pytest-qt):** toggle wiring persists/applies; Open + Export invoke the injected callables.
---
<!-- COMMENTS:END -->
